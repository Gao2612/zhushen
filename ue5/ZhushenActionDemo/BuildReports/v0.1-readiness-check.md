# v0.1 Readiness Check

本清单用于区分 v0.1 已完成、已验证、仍阻塞的事项。当前记录时间：2026-06-26。

## 已完成

- [x] UE5 v0.1 工程骨架已创建。
- [x] `Content/Core`、`Content/Demo`、`Content/Test` 目录结构已建立。
- [x] Windows / Android 基础配置已写入 `Config`。
- [x] Android 包名、应用名和横屏方向已配置。
- [x] UE 5.8 已安装在 `G:\UE_5.8`。
- [x] `UnrealEditor.exe`、`UnrealEditor-Cmd.exe`、`RunUAT.bat` 可执行。
- [x] `Tools/CreateV01StartupMap.py` 可运行。
- [x] `Content/Demo/Maps/L_v01_Startup.umap` 已生成。
- [x] 地图已包含灰盒地面、可见占位角色、相机、灯光、文字和 PlayerStart。
- [x] 已在 Unreal Editor 打开生成地图供人工查看。
- [x] Windows Development 内容版包已打出并启动验证。
- [x] Windows Shipping 内容版包已打出并启动验证。
- [x] Windows 包体大小和启动结果已记录到 `BuildReports/v0.1-build-report.md`。

## 当前未完成

- [ ] 正式 C++ 工程完整 `-build`。
- [ ] Android Development APK 生成。
- [ ] Android Shipping APK 生成。
- [ ] Android APK 真机安装。
- [ ] Android 横屏运行验证。
- [ ] Windows / Android 平均帧率采样。
- [ ] Android 启动时间、包体、发热记录。

## 当前阻塞项

### Windows C++ build

UBT 仍输出：

```text
##PlatformValidate: Win64 INVALID 10.0.22621.0
```

当前 Windows 包是内容版临时工程验证，命令使用 `-nocompile -nocompileeditor -skipbuild`。后续要补完整 Windows SDK / MSVC 后再恢复正式 C++ 构建。

### Android cook

Android SDK / NDK 已被识别为：

```text
##PlatformValidate: Android VALID r27c
```

但 Development cook 两次都卡在移动端 shader：

```text
Cooked packages 440 Packages Remain 61 Total 501
Cooker has been blocked from saving the current packages
```

日志显示等待 `InterchangeAssets`、`HairStrands`、`Paper2D` 和部分 Engine material 的 Vulkan / OpenGL ES shader。下一步应清理默认插件和 Android RHI，而不是直接继续等待。

### Android device

当前 adb 输出：

```text
List of devices attached
```

没有在线设备，无法验证安装、横屏、启动时间、帧率和发热。

## 下一步命令

检查环境：

```powershell
cd G:\zhushen-source\ue5\ZhushenActionDemo
.\Tools\check-v01-env.ps1
```

重新生成地图：

```powershell
& 'G:\UE_5.8\Engine\Binaries\Win64\UnrealEditor-Cmd.exe' `
  'G:\zhushen-source\ue5\ZhushenActionDemo\ZhushenActionDemo.uproject' `
  -run=pythonscript `
  -script='G:\zhushen-source\ue5\ZhushenActionDemo\Tools\CreateV01StartupMap.py' `
  -unattended -nop4 -nosplash -NoSound
```

下一次 Android 优化优先级：

1. 禁用 v0.1 不需要的默认插件。
2. 确认 Android cook 只保留目标 RHI。
3. 再运行 Android Development 打包。
4. 连接真机后执行安装和横屏验证。
