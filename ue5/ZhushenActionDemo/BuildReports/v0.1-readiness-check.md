# v0.1 Readiness Check

本清单用于区分“引擎安装前能做的准备”和“必须等 UE 5.8 安装后才能验证的事项”。

## 引擎安装前已完成

- [x] UE5 v0.1 工程骨架已创建。
- [x] `Content/Core`、`Content/Demo`、`Content/Test` 目录结构已建立。
- [x] C++ `GameMode`、`Pawn`、`HUD` 占位类已建立。
- [x] Windows / Android 基础配置已写入 `Config`。
- [x] Android 包名、应用名和横屏方向已配置。
- [x] 下载清单与下载入口脚本已补齐。
- [x] 桌面交付目录已保存 Epic、JDK、Android command-line tools 与 VS Build Tools 安装包。
- [x] 资料馆 UE Demo 页面状态已改为“环境准备中”。

## 引擎安装前还可做

- [x] 增强 `check-v01-env.ps1`，输出 VS、MSBuild、Windows SDK、Android Studio、SDK、NDK、JDK、adb 和 UE_ROOT 状态。
- [x] 补全 `BuildReports/v0.1-build-report.md` 的待填模板。
- [x] 静态检查 C++ 结构是否符合 v0.1 极简目标。

## 引擎安装后才能做

- [ ] 用 Unreal Editor 打开 `ZhushenActionDemo.uproject`。
- [ ] 运行 `Tools/CreateV01StartupMap.py` 生成 `L_v01_Startup.umap`。
- [ ] 在编辑器 Play 模式确认地图、占位角色、相机和 HUD 可见。
- [ ] 执行 `Tools/build-v01-windows.ps1`。
- [ ] 执行 `Tools/build-v01-android.ps1`。
- [ ] 安装 Android APK 并确认横屏启动。
- [ ] 记录 Windows / Android 包体大小、启动时间、平均帧率和问题。

## 当前阻塞项

- 当前电脑尚未安装 UE 5.8 引擎本体。
- UE 5.8 本体需要通过 Epic Games Launcher 登录账号后安装。
- 未安装完成前不能生成 `.umap`、不能编译 UE C++、不能打 Windows / Android 包。

## 下一步命令

```powershell
cd E:\zhushen\ue5\ZhushenActionDemo
.\Tools\check-v01-env.ps1
```

当 `UE_ROOT`、`UnrealEditor`、`RunUAT` 不再显示 `MISSING` 后，再执行：

```powershell
$env:UE_ROOT = "G:\UE_5.8"
.\Tools\build-v01-windows.ps1
.\Tools\build-v01-android.ps1
```
